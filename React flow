.flow-container {
  width: 300px;
  height: 50px;
  border: 1px solid #ddd;
  border-radius: 4px;
  overflow: hidden;
}

/* Node styles */
.input-node {
  background-color: #d4f1f9;
  border: 1px solid #88ccee;
  border-radius: 3px;
  padding: 2px;
  width: 60px;
  font-size: 8px;
  text-align: center;
}

.transformer-node {
  background-color: #ffe8cc;
  border: 1px solid #ffaa44;
  border-radius: 3px;
  padding: 2px;
  width: 80px;
  font-size: 8px;
  text-align: center;
}

.output-node {
  background-color: #d8f8d8;
  border: 1px solid #88dd88;
  border-radius: 3px;
  padding: 2px;
  width: 60px;
  font-size: 8px;
  text-align: center;
}

.node-header {
  font-weight: bold;
  color: #333;
}

/* Enhanced edge animation */
.animated-edge {
  animation: flowAnimation 1.5s infinite;
}

@keyframes flowAnimation {
  0% {
    stroke-dasharray: 5;
    stroke-dashoffset: 20;
  }
  100% {
    stroke-dasharray: 5;
    stroke-dashoffset: 0;
  }
}

/* Make the edge path more visible */
.react-flow__edge-path {
  stroke-width: 2;
}

/* Make the ReactFlow controls smaller */
.react-flow__controls {
  transform: scale(0.7);
  transform-origin: bottom right;
  display: none;
}

/* Hide attribution */
.react-flow__attribution {
  display: none;
}

/* Disable all interactions */
.react-flow__node {
  pointer-events: none;
}

.react-flow__edge {
  pointer-events: none;
}

.react-flow__handle {
  display: none;
}

.react-flow__background {
  pointer-events: none;
}

.react-flow__pane {
  cursor: default !important;
}

/* Hide any remaining interactive elements */
.react-flow__minimap {
  display: none;
}

.react-flow__panel {
  display: none;
}
